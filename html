<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>空手道場 - Детская школа карате</title>
    
    <!-- Tailwind CSS CDN -->
    <script src="https://cdn.tailwindcss.com"></script>
    
    <!-- React and ReactDOM -->
    <script src="https://unpkg.com/react@18/umd/react.development.js"></script>
    <script src="https://unpkg.com/react-dom@18/umd/react-dom.development.js"></script>
    
    <!-- Babel for JSX transformation -->
    <script src="https://unpkg.com/@babel/standalone/babel.min.js"></script>
    
    <!-- Framer Motion (CDN version) -->
    <script src="https://unpkg.com/framer-motion@11/dist/framer-motion.umd.js"></script>
    
    <!-- Lucide React Icons -->
    <script src="https://unpkg.com/lucide-react@latest/dist/umd/lucide-react.js"></script>

    <style>
        :root {
            --background: #ffffff;
            --foreground: #1a1a1a;
            --border: rgba(0, 0, 0, 0.1);
            --radius: 0.625rem;
        }

        * {
            box-sizing: border-box;
        }

        body {
            margin: 0;
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
            background: linear-gradient(to bottom, #f9fafb, #f3f4f6);
            color: #1a1a1a;
            overflow-x: hidden;
        }

        .sakura-petal {
            position: absolute;
            width: 10px;
            height: 10px;
            background: #ec4899;
            border-radius: 50% 0 50% 0;
            transform: rotate(45deg);
            animation: fall linear infinite;
        }

        @keyframes fall {
            0% {
                transform: translateY(-100vh) rotate(0deg);
                opacity: 1;
            }
            100% {
                transform: translateY(100vh) rotate(360deg);
                opacity: 0;
            }
        }

        .glass-effect {
            backdrop-filter: blur(10px);
            background: rgba(255, 255, 255, 0.8);
        }

        .gradient-bg {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        }

        .japanese-text {
            font-family: 'Noto Sans JP', sans-serif;
        }

        /* Loading spinner */
        .spinner {
            border: 2px solid #f3f3f3;
            border-top: 2px solid #ef4444;
            border-radius: 50%;
            width: 20px;
            height: 20px;
            animation: spin 1s linear infinite;
        }

        @keyframes spin {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }

        /* Modal backdrop */
        .modal-backdrop {
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: rgba(0, 0, 0, 0.5);
            backdrop-filter: blur(4px);
            z-index: 50;
        }

        /* Responsive adjustments */
        @media (max-width: 768px) {
            .container {
                padding: 0 1rem;
            }
        }
    </style>
</head>
<body>
    <div id="root"></div>

    <script type="text/babel">
        const { useState, useEffect, useRef } = React;
        const { motion, AnimatePresence } = window.FramerMotion;
        const { Home, Calendar, CreditCard, Trophy, ChevronLeft, ChevronRight, X, User, Users, Check, Star, Medal, Award } = window.LucideReact;

        // Sakura Animation Component
        function SakuraAnimation() {
            const [petals, setPetals] = useState([]);

            useEffect(() => {
                const petalArray = [];
                for (let i = 0; i < 20; i++) {
                    petalArray.push({
                        id: i,
                        left: Math.random() * 100,
                        animationDuration: Math.random() * 3 + 2,
                        animationDelay: Math.random() * 2
                    });
                }
                setPetals(petalArray);
            }, []);

            return (
                <div className="absolute inset-0 pointer-events-none overflow-hidden">
                    {petals.map(petal => (
                        <div
                            key={petal.id}
                            className="sakura-petal"
                            style={{
                                left: `${petal.left}%`,
                                animationDuration: `${petal.animationDuration}s`,
                                animationDelay: `${petal.animationDelay}s`
                            }}
                        />
                    ))}
                </div>
            );
        }

        // Banner Carousel Component
        function BannerCarousel() {
            const [currentIndex, setCurrentIndex] = useState(0);

            const banners = [
                {
                    id: 1,
                    title: "Скидка 20% на первый месяц!",
                    subtitle: "Для новых учеников",
                    bg: "from-red-600 to-red-700",
                    icon: "🥋"
                },
                {
                    id: 2,
                    title: "Открытый урок",
                    subtitle: "Каждую субботу в 10:00",
                    bg: "from-orange-500 to-red-600",
                    icon: "👨‍🏫"
                },
                {
                    id: 3,
                    title: "Турнир в марте",
                    subtitle: "Регистрация открыта",
                    bg: "from-red-700 to-red-800",
                    icon: "🏆"
                },
                {
                    id: 4,
                    title: "Летний лагерь",
                    subtitle: "Интенсивное обучение",
                    bg: "from-red-500 to-orange-500",
                    icon: "⛺"
                }
            ];

            useEffect(() => {
                const interval = setInterval(() => {
                    setCurrentIndex((prev) => (prev + 1) % banners.length);
                }, 5000);

                return () => clearInterval(interval);
            }, []);

            const nextSlide = () => {
                setCurrentIndex((prev) => (prev + 1) % banners.length);
            };

            const prevSlide = () => {
                setCurrentIndex((prev) => (prev - 1 + banners.length) % banners.length);
            };

            return (
                <div className="relative h-32 mx-4 mt-4 rounded-2xl overflow-hidden shadow-2xl">
                    <AnimatePresence mode="wait">
                        <motion.div
                            key={currentIndex}
                            initial={{ opacity: 0, x: 300 }}
                            animate={{ opacity: 1, x: 0 }}
                            exit={{ opacity: 0, x: -300 }}
                            transition={{ duration: 0.5, ease: "easeInOut" }}
                            className={`absolute inset-0 bg-gradient-to-r ${banners[currentIndex].bg} flex items-center justify-between px-6 text-white`}
                        >
                            <div className="flex-1">
                                <motion.h3 
                                    initial={{ opacity: 0, y: 20 }}
                                    animate={{ opacity: 1, y: 0 }}
                                    transition={{ delay: 0.2 }}
                                    className="text-lg font-semibold mb-1"
                                >
                                    {banners[currentIndex].title}
                                </motion.h3>
                                <motion.p 
                                    initial={{ opacity: 0, y: 20 }}
                                    animate={{ opacity: 1, y: 0 }}
                                    transition={{ delay: 0.3 }}
                                    className="text-sm opacity-95"
                                >
                                    {banners[currentIndex].subtitle}
                                </motion.p>
                            </div>
                            <motion.div 
                                initial={{ scale: 0, rotate: -180 }}
                                animate={{ scale: 1, rotate: 0 }}
                                transition={{ delay: 0.4, duration: 0.6 }}
                                className="text-4xl"
                            >
                                {banners[currentIndex].icon}
                            </motion.div>
                        </motion.div>
                    </AnimatePresence>

                    <button
                        onClick={prevSlide}
                        className="absolute left-2 top-1/2 transform -translate-y-1/2 text-white/80 hover:text-white transition-colors p-1 rounded-full bg-black/20 backdrop-blur-sm"
                    >
                        <ChevronLeft size={20} />
                    </button>
                    
                    <button
                        onClick={nextSlide}
                        className="absolute right-2 top-1/2 transform -translate-y-1/2 text-white/80 hover:text-white transition-colors p-1 rounded-full bg-black/20 backdrop-blur-sm"
                    >
                        <ChevronRight size={20} />
                    </button>

                    <div className="absolute bottom-2 left-1/2 transform -translate-x-1/2 flex space-x-2">
                        {banners.map((_, index) => (
                            <button
                                key={index}
                                onClick={() => setCurrentIndex(index)}
                                className={`w-2 h-2 rounded-full transition-all ${
                                    index === currentIndex ? 'bg-white scale-125' : 'bg-white/50'
                                }`}
                            />
                        ))}
                    </div>
                </div>
            );
        }

        // Karate Calendar Component
        function KarateCalendar({ onDateClick }) {
            const [currentMonth, setCurrentMonth] = useState(new Date());
            const [calendarData, setCalendarData] = useState([]);

            const generateCalendarData = () => {
                const daysInMonth = new Date(currentMonth.getFullYear(), currentMonth.getMonth() + 1, 0).getDate();
                const days = [];

                for (let i = 1; i <= daysInMonth; i++) {
                    const totalSpots = Math.floor(Math.random() * 10) + 10;
                    const availableSpots = Math.floor(Math.random() * totalSpots);
                    let status;

                    if (availableSpots === 0) {
                        status = 'full';
                    } else if (availableSpots <= totalSpots * 0.3) {
                        status = 'limited';
                    } else {
                        status = 'available';
                    }

                    days.push({
                        date: i,
                        status,
                        availableSpots,
                        totalSpots
                    });
                }

                return days;
            };

            useEffect(() => {
                setCalendarData(generateCalendarData());
            }, [currentMonth]);

            const getStatusColor = (status) => {
                switch (status) {
                    case 'available':
                        return 'bg-emerald-500 border-emerald-600 text-white shadow-lg shadow-emerald-200 hover:bg-emerald-600';
                    case 'limited':
                        return 'bg-amber-500 border-amber-600 text-white shadow-lg shadow-amber-200 hover:bg-amber-600';
                    case 'full':
                        return 'bg-gray-400 border-gray-500 text-white shadow-lg shadow-gray-200 cursor-not-allowed opacity-75';
                    default:
                        return 'bg-gray-200';
                }
            };

            const nextMonth = () => {
                setCurrentMonth(new Date(currentMonth.getFullYear(), currentMonth.getMonth() + 1));
            };

            const prevMonth = () => {
                setCurrentMonth(new Date(currentMonth.getFullYear(), currentMonth.getMonth() - 1));
            };

            const formatDate = (date) => {
                const year = currentMonth.getFullYear();
                const month = currentMonth.getMonth() + 1;
                return `${year}-${month.toString().padStart(2, '0')}-${date.toString().padStart(2, '0')}`;
            };

            const monthNames = [
                'Январь', 'Февраль', 'Март', 'Апрель', 'Май', 'Июнь',
                'Июль', 'Август', 'Сентябрь', 'Октябрь', 'Ноябрь', 'Декабрь'
            ];

            return (
                <div className="bg-white rounded-2xl shadow-xl border border-gray-100 overflow-hidden">
                    <div className="bg-gradient-to-r from-gray-50 to-gray-100 p-4">
                        <div className="flex items-center justify-between">
                            <button
                                onClick={prevMonth}
                                className="p-2 rounded-xl hover:bg-white transition-colors text-gray-600 hover:text-gray-800 shadow-sm"
                            >
                                <ChevronLeft size={20} />
                            </button>
                            
                            <motion.h3 
                                key={currentMonth.getMonth()}
                                initial={{ opacity: 0, y: -10 }}
                                animate={{ opacity: 1, y: 0 }}
                                className="text-lg text-gray-800 font-semibold"
                            >
                                {monthNames[currentMonth.getMonth()]} {currentMonth.getFullYear()}
                            </motion.h3>
                            
                            <button
                                onClick={nextMonth}
                                className="p-2 rounded-xl hover:bg-white transition-colors text-gray-600 hover:text-gray-800 shadow-sm"
                            >
                                <ChevronRight size={20} />
                            </button>
                        </div>
                    </div>

                    <div className="p-6">
                        <div className="grid grid-cols-7 gap-2 mb-4">
                            {['Пн', 'Вт', 'Ср', 'Чт', 'Пт', 'Сб', 'Вс'].map((day) => (
                                <div key={day} className="text-center text-sm font-semibold text-gray-500 py-2">
                                    {day}
                                </div>
                            ))}
                        </div>

                        <div className="grid grid-cols-7 gap-3">
                            {calendarData.map((day, index) => (
                                <motion.button
                                    key={day.date}
                                    initial={{ opacity: 0, scale: 0.8 }}
                                    animate={{ opacity: 1, scale: 1 }}
                                    transition={{ delay: index * 0.02 }}
                                    whileHover={{ scale: day.status !== 'full' ? 1.1 : 1 }}
                                    whileTap={{ scale: day.status !== 'full' ? 0.95 : 1 }}
                                    onClick={() => day.status !== 'full' && onDateClick(formatDate(day.date))}
                                    disabled={day.status === 'full'}
                                    className={`w-11 h-11 rounded-xl border-2 flex items-center justify-center text-sm font-semibold transition-all duration-200 ${getStatusColor(day.status)}`}
                                >
                                    {day.date}
                                </motion.button>
                            ))}
                        </div>

                        <div className="mt-8 p-4 bg-gray-50 rounded-xl">
                            <h4 className="font-semibold text-gray-700 mb-3 text-center">Обозначения</h4>
                            <div className="space-y-3">
                                <div className="flex items-center justify-between">
                                    <div className="flex items-center space-x-3">
                                        <div className="w-4 h-4 bg-emerald-500 rounded-full shadow-sm"></div>
                                        <span className="text-sm font-medium text-gray-700">Есть места</span>
                                    </div>
                                    <span className="text-xs text-gray-500">Свободная запись</span>
                                </div>
                                <div className="flex items-center justify-between">
                                    <div className="flex items-center space-x-3">
                                        <div className="w-4 h-4 bg-amber-500 rounded-full shadow-sm"></div>
                                        <span className="text-sm font-medium text-gray-700">Мало мест</span>
                                    </div>
                                    <span className="text-xs text-gray-500">Поторопитесь</span>
                                </div>
                                <div className="flex items-center justify-between">
                                    <div className="flex items-center space-x-3">
                                        <div className="w-4 h-4 bg-gray-400 rounded-full shadow-sm"></div>
                                        <span className="text-sm font-medium text-gray-700">Мест нет</span>
                                    </div>
                                    <span className="text-xs text-gray-500">Запись закрыта</span>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            );
        }

        // Booking Modal Component
        function BookingModal({ isOpen, onClose, selectedDate }) {
            const [childName, setChildName] = useState('');
            const [selectedGroup, setSelectedGroup] = useState('');
            const [isSubmitting, setIsSubmitting] = useState(false);
            const [showSuccess, setShowSuccess] = useState(false);

            const handleSubmit = async (e) => {
                e.preventDefault();
                if (!childName || !selectedGroup) return;

                setIsSubmitting(true);
                
                // Simulate API call
                await new Promise(resolve => setTimeout(resolve, 1500));
                
                setIsSubmitting(false);
                setShowSuccess(true);
                
                // Reset form and close after animation
                setTimeout(() => {
                    setShowSuccess(false);
                    setChildName('');
                    setSelectedGroup('');
                    onClose();
                }, 3000);
            };

            const formatDate = (dateString) => {
                if (!dateString) return '';
                const date = new Date(dateString);
                return date.toLocaleDateString('ru-RU', {
                    day: 'numeric',
                    month: 'long',
                    year: 'numeric'
                });
            };

            if (!isOpen) return null;

            return (
                <>
                    <motion.div
                        initial={{ opacity: 0 }}
                        animate={{ opacity: 1 }}
                        exit={{ opacity: 0 }}
                        onClick={onClose}
                        className="modal-backdrop"
                    />

                    <motion.div
                        initial={{ y: '100%', opacity: 0 }}
                        animate={{ y: 0, opacity: 1 }}
                        exit={{ y: '100%', opacity: 0 }}
                        transition={{ type: 'spring', damping: 25, stiffness: 200 }}
                        className="fixed bottom-0 left-0 right-0 bg-white rounded-t-3xl z-50 max-h-[80vh] overflow-hidden shadow-2xl"
                    >
                        <AnimatePresence>
                            {showSuccess && (
                                <motion.div
                                    initial={{ opacity: 0, scale: 0.8 }}
                                    animate={{ opacity: 1, scale: 1 }}
                                    exit={{ opacity: 0, scale: 0.8 }}
                                    className="absolute inset-0 bg-white rounded-t-3xl flex flex-col items-center justify-center z-10"
                                >
                                    <SakuraAnimation />
                                    <motion.div
                                        initial={{ y: 20, opacity: 0 }}
                                        animate={{ y: 0, opacity: 1 }}
                                        transition={{ delay: 0.5 }}
                                        className="text-center"
                                    >
                                        <div className="text-6xl mb-4">🥋</div>
                                        <h3 className="text-2xl text-emerald-600 mb-2 font-semibold">Запись успешна!</h3>
                                        <p className="text-gray-600">Ждем вас на занятии</p>
                                    </motion.div>
                                </motion.div>
                            )}
                        </AnimatePresence>

                        <div className="p-6">
                            <div className="flex items-center justify-between mb-6">
                                <div className="flex items-center space-x-3">
                                    <div className="p-3 bg-gradient-to-r from-red-500 to-red-600 rounded-2xl text-white shadow-lg">
                                        <Users className="w-5 h-5" />
                                    </div>
                                    <div>
                                        <h3 className="text-lg text-gray-800 font-semibold">Запись на занятие</h3>
                                        <p className="text-sm text-gray-500">{formatDate(selectedDate)}</p>
                                    </div>
                                </div>
                                <button
                                    onClick={onClose}
                                    className="p-2 hover:bg-gray-100 rounded-xl transition-colors"
                                >
                                    <X size={20} className="text-gray-500" />
                                </button>
                            </div>

                            <form onSubmit={handleSubmit} className="space-y-6">
                                <div className="space-y-2">
                                    <label className="flex items-center space-x-2 text-sm font-medium">
                                        <User size={16} />
                                        <span>Имя ребёнка</span>
                                    </label>
                                    <input
                                        value={childName}
                                        onChange={(e) => setChildName(e.target.value)}
                                        placeholder="Введите имя ребёнка"
                                        className="w-full p-3 rounded-xl border border-gray-200 focus:border-red-400 focus:outline-none transition-colors"
                                        required
                                    />
                                </div>

                                <div className="space-y-2">
                                    <label className="text-sm font-medium">Группа</label>
                                    <select
                                        value={selectedGroup}
                                        onChange={(e) => setSelectedGroup(e.target.value)}
                                        className="w-full p-3 rounded-xl border border-gray-200 focus:border-red-400 focus:outline-none transition-colors"
                                        required
                                    >
                                        <option value="">Выберите группу</option>
                                        <option value="junior">Младшая группа (4-6 лет)</option>
                                        <option value="middle">Средняя группа (7-10 лет)</option>
                                        <option value="senior">Старшая группа (11-14 лет)</option>
                                    </select>
                                </div>

                                <motion.button
                                    type="submit"
                                    disabled={isSubmitting || !childName || !selectedGroup}
                                    whileTap={{ scale: 0.98 }}
                                    className="w-full bg-gradient-to-r from-red-500 to-red-600 hover:from-red-600 hover:to-red-700 text-white py-3 rounded-2xl shadow-lg hover:shadow-xl transition-all duration-200 disabled:opacity-50"
                                >
                                    {isSubmitting ? (
                                        <div className="spinner mx-auto"></div>
                                    ) : (
                                        'Записаться'
                                    )}
                                </motion.button>
                            </form>

                            <div className="flex justify-center mt-6">
                                <div className="text-red-400 text-2xl">桜</div>
                            </div>
                        </div>
                    </motion.div>
                </>
            );
        }

        // Home Page Component
        function HomePage() {
            const [selectedDate, setSelectedDate] = useState(null);
            const [isBookingOpen, setIsBookingOpen] = useState(false);

            const handleDateClick = (date) => {
                setSelectedDate(date);
                setIsBookingOpen(true);
            };

            const handleBookingClose = () => {
                setIsBookingOpen(false);
                setSelectedDate(null);
            };

            return (
                <div className="min-h-screen">
                    <div className="bg-gradient-to-r from-gray-900 to-black text-white">
                        <div className="p-6">
                            <motion.h1 
                                initial={{ opacity: 0, y: -20 }}
                                animate={{ opacity: 1, y: 0 }}
                                className="text-center text-2xl font-light tracking-wider"
                            >
                                空手道場
                            </motion.h1>
                            <motion.p 
                                initial={{ opacity: 0, y: -10 }}
                                animate={{ opacity: 1, y: 0 }}
                                transition={{ delay: 0.2 }}
                                className="text-center text-sm opacity-90 mt-1 tracking-wide"
                            >
                                Детская школа карате
                            </motion.p>
                            <motion.div
                                initial={{ scaleX: 0 }}
                                animate={{ scaleX: 1 }}
                                transition={{ delay: 0.4, duration: 0.6 }}
                                className="w-16 h-px bg-gradient-to-r from-transparent via-red-500 to-transparent mx-auto mt-3"
                            />
                        </div>
                    </div>

                    <BannerCarousel />

                    <div className="p-4">
                        <motion.div
                            initial={{ opacity: 0, x: -20 }}
                            animate={{ opacity: 1, x: 0 }}
                            transition={{ delay: 0.3 }}
                            className="mb-6"
                        >
                            <h2 className="text-xl text-center text-gray-800 mb-4">
                                Расписание занятий
                            </h2>
                        </motion.div>
                        
                        <KarateCalendar onDateClick={handleDateClick} />

                        <motion.div
                            initial={{ opacity: 0, y: 30 }}
                            animate={{ opacity: 1, y: 0 }}
                            transition={{ delay: 0.6 }}
                            className="mt-6 bg-white rounded-2xl shadow-xl border border-gray-100"
                        >
                            <div className="bg-gradient-to-r from-gray-50 to-gray-100 rounded-t-2xl p-4">
                                <h3 className="font-semibold text-gray-800 text-center">
                                    Важная информация
                                </h3>
                            </div>
                            <div className="p-6 space-y-4">
                                <div className="flex items-center space-x-3 p-4 rounded-xl bg-red-50 border border-red-100">
                                    <div className="w-10 h-10 rounded-full bg-red-100 flex items-center justify-center">
                                        <span className="text-lg">⚠️</span>
                                    </div>
                                    <p className="text-sm text-gray-700 font-medium">
                                        Приходите на 10 минут раньше для переодевания
                                    </p>
                                </div>
                                <div className="flex items-center space-x-3 p-4 rounded-xl bg-blue-50 border border-blue-100">
                                    <div className="w-10 h-10 rounded-full bg-blue-100 flex items-center justify-center">
                                        <span className="text-lg">🥋</span>
                                    </div>
                                    <p className="text-sm text-gray-700 font-medium">
                                        Форма обязательна для всех занятий
                                    </p>
                                </div>
                                <div className="flex items-center space-x-3 p-4 rounded-xl bg-green-50 border border-green-100">
                                    <div className="w-10 h-10 rounded-full bg-green-100 flex items-center justify-center">
                                        <span className="text-lg">📞</span>
                                    </div>
                                    <p className="text-sm text-gray-700 font-medium">
                                        О пропуске занятия сообщайте заранее
                                    </p>
                                </div>
                            </div>
                        </motion.div>
                    </div>

                    <BookingModal 
                        isOpen={isBookingOpen}
                        onClose={handleBookingClose}
                        selectedDate={selectedDate}
                    />
                </div>
            );
        }

        // Booking Page Component
        function BookingPage() {
            const [selectedDate, setSelectedDate] = useState(null);
            const [isBookingOpen, setIsBookingOpen] = useState(false);

            const handleDateClick = (date) => {
                setSelectedDate(date);
                setIsBookingOpen(true);
            };

            const handleBookingClose = () => {
                setIsBookingOpen(false);
                setSelectedDate(null);
            };

            return (
                <div className="min-h-screen bg-gradient-to-b from-gray-50 to-gray-100">
                    <div className="bg-gradient-to-r from-gray-900 to-black text-white p-6">
                        <motion.h1 
                            initial={{ opacity: 0, y: -20 }}
                            animate={{ opacity: 1, y: 0 }}
                            className="text-center text-2xl font-light tracking-wider"
                        >
                            予約
                        </motion.h1>
                        <motion.p 
                            initial={{ opacity: 0, y: -10 }}
                            animate={{ opacity: 1, y: 0 }}
                            transition={{ delay: 0.2 }}
                            className="text-center text-sm opacity-90 mt-1"
                        >
                            Запись на занятия
                        </motion.p>
                    </div>

                    <div className="p-4">
                        <div className="grid grid-cols-1 md:grid-cols-3 gap-4 mb-6">
                            <motion.div
                                initial={{ opacity: 0, y: 20 }}
                                animate={{ opacity: 1, y: 0 }}
                                transition={{ delay: 0.1 }}
                                className="bg-white rounded-2xl p-4 shadow-lg border border-emerald-100"
                            >
                                <div className="flex items-center space-x-3">
                                    <div className="w-4 h-4 rounded-full bg-emerald-500 shadow-sm"></div>
                                    <div>
                                        <p className="font-semibold text-emerald-700">Есть места</p>
                                        <p className="text-sm text-gray-600">Свободная запись</p>
                                    </div>
                                </div>
                            </motion.div>

                            <motion.div
                                initial={{ opacity: 0, y: 20 }}
                                animate={{ opacity: 1, y: 0 }}
                                transition={{ delay: 0.2 }}
                                className="bg-white rounded-2xl p-4 shadow-lg border border-amber-100"
                            >
                                <div className="flex items-center space-x-3">
                                    <div className="w-4 h-4 rounded-full bg-amber-500 shadow-sm"></div>
                                    <div>
                                        <p className="font-semibold text-amber-700">Мало мест</p>
                                        <p className="text-sm text-gray-600">Торопитесь записаться</p>
                                    </div>
                                </div>
                            </motion.div>

                            <motion.div
                                initial={{ opacity: 0, y: 20 }}
                                animate={{ opacity: 1, y: 0 }}
                                transition={{ delay: 0.3 }}
                                className="bg-white rounded-2xl p-4 shadow-lg border border-gray-200"
                            >
                                <div className="flex items-center space-x-3">
                                    <div className="w-4 h-4 rounded-full bg-gray-400 shadow-sm"></div>
                                    <div>
                                        <p className="font-semibold text-gray-700">Мест нет</p>
                                        <p className="text-sm text-gray-600">Запись закрыта</p>
                                    </div>
                                </div>
                            </motion.div>
                        </div>

                        <motion.div
                            initial={{ opacity: 0, y: 30 }}
                            animate={{ opacity: 1, y: 0 }}
                            transition={{ delay: 0.4 }}
                        >
                            <KarateCalendar onDateClick={handleDateClick} />
                        </motion.div>

                        <motion.div
                            initial={{ opacity: 0, y: 30 }}
                            animate={{ opacity: 1, y: 0 }}
                            transition={{ delay: 0.5 }}
                            className="mt-6 bg-white rounded-2xl p-6 shadow-xl border border-gray-100"
                        >
                            <h3 className="text-lg font-semibold text-gray-800 mb-4">Расписание групп</h3>
                            <div className="space-y-3">
                                <div className="flex justify-between items-center p-4 bg-red-50 rounded-xl border border-red-100">
                                    <span className="font-semibold text-gray-800">Младшая группа (4-6 лет)</span>
                                    <span className="text-sm text-gray-600 font-medium">16:00 - 17:00</span>
                                </div>
                                <div className="flex justify-between items-center p-4 bg-red-50 rounded-xl border border-red-100">
                                    <span className="font-semibold text-gray-800">Средняя группа (7-10 лет)</span>
                                    <span className="text-sm text-gray-600 font-medium">17:00 - 18:00</span>
                                </div>
                                <div className="flex justify-between items-center p-4 bg-red-50 rounded-xl border border-red-100">
                                    <span className="font-semibold text-gray-800">Старшая группа (11-14 лет)</span>
                                    <span className="text-sm text-gray-600 font-medium">18:00 - 19:00</span>
                                </div>
                            </div>
                        </motion.div>
                    </div>

                    <BookingModal 
                        isOpen={isBookingOpen}
                        onClose={handleBookingClose}
                        selectedDate={selectedDate}
                    />
                </div>
            );
        }

        // Payment Page Component
        function PaymentPage() {
            const [selectedPlan, setSelectedPlan] = useState(null);
            const [isProcessing, setIsProcessing] = useState(false);
            const [showSuccess, setShowSuccess] = useState(false);

            const plans = [
                {
                    id: 'trial',
                    name: 'Пробное занятие',
                    price: 500,
                    duration: '1 занятие',
                    features: ['Знакомство с тренером', 'Базовые техники', 'Оценка уровня'],
                    color: 'from-blue-500 to-blue-600'
                },
                {
                    id: 'basic',
                    name: 'Базовый',
                    price: 3000,
                    duration: '4 занятия',
                    features: ['4 занятия в месяц', 'Персональные рекомендации', 'Доступ к материалам'],
                    color: 'from-emerald-500 to-emerald-600'
                },
                {
                    id: 'standard',
                    name: 'Стандартный',
                    price: 5000,
                    duration: '8 занятий',
                    features: ['8 занятий в месяц', 'Участие в соревнованиях', 'Индивидуальные консультации', 'Сертификат прогресса'],
                    popular: true,
                    color: 'from-red-500 to-red-600'
                },
                {
                    id: 'premium',
                    name: 'Премиум',
                    price: 8000,
                    duration: '12 занятий',
                    features: ['12 занятий в месяц', 'Персональный тренинг', 'Экипировка в подарок', 'Приоритетная запись', 'Мастер-классы'],
                    color: 'from-purple-500 to-purple-600'
                }
            ];

            const handlePayment = async (planId) => {
                setSelectedPlan(planId);
                setIsProcessing(true);

                await new Promise(resolve => setTimeout(resolve, 2000));

                setIsProcessing(false);
                setShowSuccess(true);

                setTimeout(() => {
                    setShowSuccess(false);
                    setSelectedPlan(null);
                }, 3000);
            };

            return (
                <div className="min-h-screen bg-gradient-to-b from-gray-50 to-gray-100">
                    <div className="bg-gradient-to-r from-gray-900 to-black text-white p-6">
                        <motion.h1 
                            initial={{ opacity: 0, y: -20 }}
                            animate={{ opacity: 1, y: 0 }}
                            className="text-center text-2xl font-light tracking-wider"
                        >
                            支払い
                        </motion.h1>
                        <motion.p 
                            initial={{ opacity: 0, y: -10 }}
                            animate={{ opacity: 1, y: 0 }}
                            transition={{ delay: 0.2 }}
                            className="text-center text-sm opacity-90 mt-1"
                        >
                            Выберите абонемент
                        </motion.p>
                    </div>

                    <AnimatePresence>
                        {showSuccess && (
                            <motion.div
                                initial={{ opacity: 0 }}
                                animate={{ opacity: 1 }}
                                exit={{ opacity: 0 }}
                                className="fixed inset-0 bg-white flex flex-col items-center justify-center z-50"
                            >
                                <SakuraAnimation />
                                <motion.div
                                    initial={{ scale: 0, rotate: -180 }}
                                    animate={{ scale: 1, rotate: 0 }}
                                    transition={{ delay: 0.5, type: "spring", stiffness: 200 }}
                                    className="text-center"
                                >
                                    <div className="text-6xl mb-4">💳</div>
                                    <h3 className="text-2xl text-emerald-600 mb-2 font-semibold">Оплата успешна!</h3>
                                    <p className="text-gray-600">Ваш абонемент активирован</p>
                                </motion.div>
                            </motion.div>
                        )}
                    </AnimatePresence>

                    <div className="p-4">
                        <div className="grid grid-cols-1 md:grid-cols-2 gap-4 mb-6">
                            {plans.map((plan, index) => (
                                <motion.div
                                    key={plan.id}
                                    initial={{ opacity: 0, y: 20 }}
                                    animate={{ opacity: 1, y: 0 }}
                                    transition={{ delay: index * 0.1 }}
                                    className={`relative bg-white rounded-2xl shadow-xl overflow-hidden border ${
                                        plan.popular ? 'border-red-200 ring-2 ring-red-100' : 'border-gray-100'
                                    }`}
                                >
                                    {plan.popular && (
                                        <motion.div
                                            initial={{ scale: 0 }}
                                            animate={{ scale: 1 }}
                                            transition={{ delay: 0.5 }}
                                            className="absolute top-4 right-4 bg-red-500 text-white px-3 py-1 rounded-full text-xs flex items-center space-x-1 shadow-lg"
                                        >
                                            <Star size={12} />
                                            <span>Популярный</span>
                                        </motion.div>
                                    )}

                                    <div className={`bg-gradient-to-r ${plan.color} p-6 text-white`}>
                                        <h3 className="text-xl font-semibold mb-2">{plan.name}</h3>
                                        <div className="flex items-baseline space-x-1">
                                            <span className="text-3xl font-light">{plan.price.toLocaleString()}</span>
                                            <span className="text-sm opacity-80">₽</span>
                                        </div>
                                        <p className="text-sm opacity-90 mt-1">{plan.duration}</p>
                                    </div>

                                    <div className="p-6">
                                        <ul className="space-y-3 mb-6">
                                            {plan.features.map((feature, featureIndex) => (
                                                <motion.li
                                                    key={featureIndex}
                                                    initial={{ opacity: 0, x: -20 }}
                                                    animate={{ opacity: 1, x: 0 }}
                                                    transition={{ delay: index * 0.1 + featureIndex * 0.1 }}
                                                    className="flex items-center space-x-3"
                                                >
                                                    <Check size={16} className="text-emerald-500 flex-shrink-0" />
                                                    <span className="text-sm text-gray-700">{feature}</span>
                                                </motion.li>
                                            ))}
                                        </ul>

                                        <button
                                            onClick={() => handlePayment(plan.id)}
                                            disabled={isProcessing}
                                            className={`w-full bg-gradient-to-r ${plan.color} hover:opacity-90 text-white py-3 rounded-2xl shadow-lg hover:shadow-xl transition-all duration-200 disabled:opacity-50`}
                                        >
                                            {isProcessing && selectedPlan === plan.id ? (
                                                <div className="spinner mx-auto"></div>
                                            ) : (
                                                <div className="flex items-center justify-center space-x-2">
                                                    <CreditCard size={16} />
                                                    <span>Оплатить</span>
                                                </div>
                                            )}
                                        </button>
                                    </div>
                                </motion.div>
                            ))}
                        </div>

                        <motion.div
                            initial={{ opacity: 0, y: 30 }}
                            animate={{ opacity: 1, y: 0 }}
                            transition={{ delay: 0.6 }}
                            className="bg-white rounded-2xl p-6 shadow-xl border border-gray-100"
                        >
                            <h3 className="text-lg font-semibold text-gray-800 mb-4">Информация об оплате</h3>
                            <div className="space-y-3 text-sm text-gray-600">
                                <p>• Оплата производится через безопасную платежную систему</p>
                                <p>• Абонемент активируется сразу после оплаты</p>
                                <p>• Возможен возврат средств в течение 14 дней</p>
                                <p>• При возникновении вопросов обращайтесь к администратору</p>
                            </div>
                        </motion.div>

                        <div className="flex justify-center mt-6">
                            <div className="text-red-400 text-3xl">武</div>
                        </div>
                    </div>
                </div>
            );
        }

        // Rating Page Component
        function RatingPage() {
            const [activeGroup, setActiveGroup] = useState('junior');

            const mockData = {
                junior: [
                    { id: '1', name: 'Алиса Петров', belt: 'yellow', points: 850, rank: 1 },
                    { id: '2', name: 'Максим Иванов', belt: 'yellow', points: 780, rank: 2 },
                    { id: '3', name: 'София Смирнова', belt: 'white', points: 720, rank: 3 },
                    { id: '4', name: 'Даниил Козлов', belt: 'white', points: 680, rank: 4 },
                    { id: '5', name: 'Мария Попова', belt: 'white', points: 620, rank: 5 }
                ],
                middle: [
                    { id: '6', name: 'Артем Федоров', belt: 'green', points: 950, rank: 1 },
                    { id: '7', name: 'Анна Морозова', belt: 'orange', points: 900, rank: 2 },
                    { id: '8', name: 'Никита Волков', belt: 'orange', points: 850, rank: 3 },
                    { id: '9', name: 'Екатерина Лебедева', belt: 'yellow', points: 800, rank: 4 },
                    { id: '10', name: 'Михаил Орлов', belt: 'yellow', points: 750, rank: 5 }
                ],
                senior: [
                    { id: '11', name: 'Александр Новиков', belt: 'brown', points: 1200, rank: 1 },
                    { id: '12', name: 'Дарья Соколова', belt: 'blue', points: 1150, rank: 2 },
                    { id: '13', name: 'Егор Павлов', belt: 'blue', points: 1100, rank: 3 },
                    { id: '14', name: 'Полина Захарова', belt: 'green', points: 1050, rank: 4 },
                    { id: '15', name: 'Владислав Титов', belt: 'green', points: 1000, rank: 5 }
                ]
            };

            const groupNames = {
                junior: 'Младшая группа (4-6 лет)',
                middle: 'Средняя группа (7-10 лет)',
                senior: 'Старшая группа (11-14 лет)'
            };

            const beltColors = {
                white: 'bg-gray-100 border-gray-300',
                yellow: 'bg-yellow-400 border-yellow-500',
                orange: 'bg-orange-400 border-orange-500',
                green: 'bg-emerald-400 border-emerald-500',
                blue: 'bg-blue-400 border-blue-500',
                brown: 'bg-amber-600 border-amber-700',
                black: 'bg-gray-900 border-gray-800'
            };

            const getRankIcon = (rank) => {
                switch (rank) {
                    case 1:
                        return React.createElement(Trophy, { className: "w-5 h-5 text-yellow-500" });
                    case 2:
                        return React.createElement(Medal, { className: "w-5 h-5 text-gray-400" });
                    case 3:
                        return React.createElement(Award, { className: "w-5 h-5 text-amber-600" });
                    default:
                        return React.createElement("div", { 
                            className: "w-5 h-5 flex items-center justify-center text-gray-500 font-semibold text-sm" 
                        }, rank);
                }
            };

            return (
                <div className="min-h-screen bg-gradient-to-b from-gray-50 to-gray-100">
                    <div className="bg-gradient-to-r from-gray-900 to-black text-white p-6">
                        <motion.h1 
                            initial={{ opacity: 0, y: -20 }}
                            animate={{ opacity: 1, y: 0 }}
                            className="text-center text-2xl font-light tracking-wider"
                        >
                            ランキング
                        </motion.h1>
                        <motion.p 
                            initial={{ opacity: 0, y: -10 }}
                            animate={{ opacity: 1, y: 0 }}
                            transition={{ delay: 0.2 }}
                            className="text-center text-sm opacity-90 mt-1"
                        >
                            Рейтинг учеников
                        </motion.p>
                    </div>

                    <div className="p-4">
                        <div className="flex space-x-1 mb-6 bg-white rounded-2xl p-1 shadow-xl border border-gray-100">
                            {Object.keys(groupNames).map((group) => (
                                <button
                                    key={group}
                                    onClick={() => setActiveGroup(group)}
                                    className={`flex-1 py-3 px-4 rounded-xl text-sm font-semibold transition-all duration-200 ${
                                        activeGroup === group
                                            ? 'bg-red-500 text-white shadow-lg'
                                            : 'text-gray-600 hover:bg-gray-50'
                                    }`}
                                >
                                    {groupNames[group].split(' ')[0]}
                                </button>
                            ))}
                        </div>

                        <motion.div
                            key={activeGroup}
                            initial={{ opacity: 0, y: 20 }}
                            animate={{ opacity: 1, y: 0 }}
                            transition={{ duration: 0.3 }}
                            className="bg-white rounded-2xl shadow-xl overflow-hidden border border-gray-100"
                        >
                            <div className="p-4 bg-gradient-to-r from-red-500 to-red-600 text-white">
                                <h3 className="text-lg font-semibold">{groupNames[activeGroup]}</h3>
                            </div>

                            <div className="divide-y divide-gray-100">
                                {mockData[activeGroup].map((student, index) => (
                                    <motion.div
                                        key={student.id}
                                        initial={{ opacity: 0, x: -20 }}
                                        animate={{ opacity: 1, x: 0 }}
                                        transition={{ delay: index * 0.1 }}
                                        className="p-4 hover:bg-gray-50 transition-colors"
                                    >
                                        <div className="flex items-center space-x-4">
                                            <div className="flex-shrink-0">
                                                {getRankIcon(student.rank)}
                                            </div>

                                            <div className={`w-8 h-4 rounded-sm border-2 ${beltColors[student.belt]} flex-shrink-0 shadow-sm`}></div>

                                            <div className="flex-1">
                                                <p className="font-semibold text-gray-800">{student.name}</p>
                                                <p className="text-sm text-gray-600">{student.points} очков</p>
                                            </div>

                                            <div className="text-right">
                                                <div className={`text-2xl font-light ${
                                                    student.rank === 1 ? 'text-yellow-500' :
                                                    student.rank === 2 ? 'text-gray-400' :
                                                    student.rank === 3 ? 'text-amber-600' :
                                                    'text-gray-500'
                                                }`}>
                                                    #{student.rank}
                                                </div>
                                            </div>
                                        </div>
                                    </motion.div>
                                ))}
                            </div>
                        </motion.div>

                        <motion.div
                            initial={{ opacity: 0, y: 30 }}
                            animate={{ opacity: 1, y: 0 }}
                            transition={{ delay: 0.5 }}
                            className="mt-6 bg-white rounded-2xl p-6 shadow-xl border border-gray-100"
                        >
                            <h3 className="text-lg font-semibold text-gray-800 mb-4">Система поясов</h3>
                            <div className="grid grid-cols-2 gap-3">
                                {Object.entries(beltColors).map(([belt, colorClass]) => (
                                    <div key={belt} className="flex items-center space-x-3">
                                        <div className={`w-6 h-3 rounded-sm border-2 ${colorClass} shadow-sm`}></div>
                                        <span className="text-sm text-gray-600 font-medium capitalize">
                                            {belt === 'white' ? 'Белый' :
                                             belt === 'yellow' ? 'Жёлтый' :
                                             belt === 'orange' ? 'Оранжевый' :
                                             belt === 'green' ? 'Зелёный' :
                                             belt === 'blue' ? 'Синий' :
                                             belt === 'brown' ? 'Коричневый' :
                                             'Чёрный'}
                                        </span>
                                    </div>
                                ))}
                            </div>
                        </motion.div>

                        <div className="flex justify-center mt-6">
                            <div className="text-red-400 text-3xl">段</div>
                        </div>
                    </div>
                </div>
            );
        }

        // Main App Component
        function App() {
            const [currentPage, setCurrentPage] = useState('home');

            const pages = {
                home: React.createElement(HomePage),
                booking: React.createElement(BookingPage),
                payment: React.createElement(PaymentPage),
                rating: React.createElement(RatingPage)
            };

            const menuItems = [
                { id: 'home', icon: Home, label: 'Главная' },
                { id: 'booking', icon: Calendar, label: 'Запись' },
                { id: 'payment', icon: CreditCard, label: 'Оплата' },
                { id: 'rating', icon: Trophy, label: 'Рейтинг' }
            ];

            return React.createElement(
                'div',
                { className: "min-h-screen bg-gradient-to-b from-gray-50 to-gray-100 flex flex-col" },
                
                React.createElement(
                    'div',
                    { className: "flex-1 pb-20" },
                    React.createElement(
                        AnimatePresence,
                        { mode: "wait" },
                        React.createElement(
                            motion.div,
                            {
                                key: currentPage,
                                initial: { opacity: 0, x: 20 },
                                animate: { opacity: 1, x: 0 },
                                exit: { opacity: 0, x: -20 },
                                transition: { duration: 0.3, ease: "easeInOut" },
                                className: "h-full"
                            },
                            pages[currentPage]
                        )
                    )
                ),

                React.createElement(
                    'div',
                    { className: "fixed bottom-0 left-0 right-0 bg-white/95 backdrop-blur-md border-t border-gray-200 shadow-2xl" },
                    React.createElement(
                        'div',
                        { className: "flex items-center justify-around py-2" },
                        menuItems.map((item) => {
                            const isActive = currentPage === item.id;
                            
                            return React.createElement(
                                motion.button,
                                {
                                    key: item.id,
                                    onClick: () => setCurrentPage(item.id),
                                    className: `flex flex-col items-center p-3 rounded-2xl transition-all duration-200 ${
                                        isActive ? 'text-red-600' : 'text-gray-500'
                                    }`,
                                    whileTap: { scale: 0.95 },
                                    whileHover: { scale: 1.05 }
                                },
                                React.createElement(
                                    motion.div,
                                    {
                                        animate: { 
                                            scale: isActive ? 1.1 : 1,
                                            backgroundColor: isActive ? '#dc2626' : 'transparent'
                                        },
                                        transition: { duration: 0.2 },
                                        className: `p-2 rounded-xl transition-all ${
                                            isActive 
                                                ? 'bg-red-600 text-white shadow-lg shadow-red-200' 
                                                : 'hover:bg-gray-100'
                                        }`
                                    },
                                    React.createElement(item.icon, { size: 22, strokeWidth: 2 })
                                ),
                                React.createElement(
                                    'span',
                                    {
                                        className: `text-xs mt-1 transition-all ${
                                            isActive ? 'font-semibold' : 'font-medium'
                                        }`
                                    },
                                    item.label
                                ),
                                isActive && React.createElement(
                                    motion.div,
                                    {
                                        layoutId: "activeIndicator",
                                        className: "absolute -top-1 w-1 h-1 bg-red-600 rounded-full",
                                        initial: false
                                    }
                                )
                            );
                        })
                    )
                )
            );
        }

        // Render the app
        const root = ReactDOM.createRoot(document.getElementById('root'));
        root.render(React.createElement(App));
    </script>
</body>
</html>
